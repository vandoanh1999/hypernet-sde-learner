# This is an example environment file.
# In a real deployment, copy this to a .env file and replace the placeholder
# with a strong, securely generated secret key.

API_KEY="your_secret_api_key_goes_here"
